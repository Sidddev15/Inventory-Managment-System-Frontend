import React from 'react';
import { Typography } from '@mui/material';

const InventoryPage: React.FC = () => {
  return <Typography variant="h6">Inventory</Typography>;
};
export default InventoryPage;
