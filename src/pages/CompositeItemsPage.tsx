import { Typography } from '@mui/material';
import React from 'react';

const CompositeItemsPage: React.FC = () => {
  return <Typography variant="h6">Composite Items</Typography>;
};

export default CompositeItemsPage;
