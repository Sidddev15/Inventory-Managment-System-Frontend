import { Typography } from '@mui/material';
import React from 'react';

const PriceListsPage: React.FC = () => {
  return <Typography variant="h6">Price Lists</Typography>;
};

export default PriceListsPage;
